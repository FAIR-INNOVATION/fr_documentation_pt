version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - git-lfs
  jobs:
    pre_build:
      - git lfs install
      - git lfs pull

sphinx:
  configuration: source/conf.py

python:
  install:
    - requirements: requirements.txt